Core Deep Learning Framework

torch>=2.0.0
torchvision>=0.15.0

Computer Vision & Image Processing

opencv-python>=4.8.0
Pillow>=10.0.0
numpy<2.0.0

RF-DETR Specifics

Note: If 'rfdetr' is not on PyPI, you might need to install via git:

git+https://www.google.com/search?q=https://github.com/ternaus/rfdetr.git

rfdetr
timm>=0.9.0
transformers>=4.30.0
accelerate

Visualization & Metrics (Crucial for predict.py and evaluate.py)

Version 0.18+ is required for the Annotator syntax used in your scripts

supervision>=0.18.0
matplotlib

Dataset & Utilities

pycocotools
tqdm
scipy
pyyaml
requests

Optional: If you used Roboflow to download data

roboflow
